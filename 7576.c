#include <stdio.h>

int main(void)
{
    int N, M, i, j;
    scanf("%d %d", &N, &M);

    int tomato[N][M];
    for(i=0; i<N; i+)
    {
        for(j=0; j<M; j++)
        {
            scanf("%d", &tomato[i][j]);
        }
    }

    
    

    return 0;
}