#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int N, i;
    int *A;
    int *B;

    A = malloc(N*sizeof(int));
    B = malloc(N*sizeof(int));

    scanf("%d", &N);
    


    return 0;
}