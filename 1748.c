#include <stdio.h>

int main(void)
{
    int N, i, anw=0;
    scanf("%d", &N);

    1~9
    10~99
    100~999

    for(i=0; i<N; i++)
    {
        if(N-9*10^i < 0)
        {
            anw += N -
        }
        else
        {
            anw += 9*10^i;    
        }
        
    }

    return 0;
}