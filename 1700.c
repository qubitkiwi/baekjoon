#include <stdio.h>

int main(void)
{
    int N, K, i;
    scanf("%d %d", &N, &K);

    int port[N] = {0,};
    int list[K];

    for(i=0; i<K; i++)
    {
        scanf("%d", &list[i]);
    }

    
    
    return 0;
}